require_relative '../lib/app'

describe "print crypto" do
    it "test" do
        expect(cours_crypto()).to eq(cours_crypto)
      end
    end

describe "array" do
    it "test2" do
        expect(array(0)).to eq(array(0))
    end
end

describe "perform" do
    it "test3" do
        expect(perform).to eq(perform)
    end
end
